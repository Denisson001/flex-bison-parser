#pragma once

#include <string>
#include <map>

typedef int                            number_t;
typedef std::string                    variable_t;
typedef std::map<variable_t, number_t> dictionary_t;
